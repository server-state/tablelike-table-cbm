import TablelikeTableCBM from './tablelike-table-cbm';

function isCompatible(data) {
    return data ? 1 : 0;  // TODO: Implement this
}

export default {
    component: TablelikeTableCBM,
    isCompatible
};
